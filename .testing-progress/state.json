{
  "schema_version": 1,
  "project": "pyauditor",
  "last_run": "2026-08-22T22:30:00Z",
  "baseline": {
    "tests": 559,
    "branch_coverage_percent": 89.29,
    "failures": 0,
    "skipped": 34
  },
  "completed_objectives": [
    "wayfinder migracao-ty/04 (fatia ty, fecha o ticket): os 150 diagnósticos do `ty check` foram zerados. 11 dos 32 `# type: ignore` legados eram mortos sob o ty — removidos. 3 sites de dívida real em src corrigidos sem supressão: engine/discovery.py (cast Literal), cli/consolidate.py (cast dict[str,object], dict é invariante), excel/capa.py (migrado para ty:ignore com a justificativa já existente — stub de Cell.value mais estreito que o runtime). 18 sites em tests/ migrados para `# ty: ignore[<rule>]` (testes deliberados de erro de runtime — **kwargs mal-tipados, atribuição em pydantic frozen, subscript em Mapping read-only); os 8 sites de write_sheet(**kwargs) em test_inms_1_1_audit.py cobrem sozinhos 12-14 diagnósticos de overload cada. 11 diagnósticos novos de ruído de stub types-openpyxl (cell.row: int|None) resolvidos com `assert <row> is not None` após o `next(...)` que deriva a linha, em vez de suprimir — nunca é None em runtime. `uv run ty check` → All checks passed em src+tests. Únicos diffs de produção são 2 `cast()` (no-op em runtime); pytest 559 passed/34 skipped/89.29% branch, sem mudança de comportamento.",
    "wayfinder migracao-ty/03: formalizada a resolução do ticket (config já estava em pyproject.toml desde commit wip anterior, nunca fechada) — perfil ty sem overrides de regra, escopo include=[src,tests]/exclude=[.scratch], respect-type-ignore-comments=false, ANN/PYI+preview já ligados no ruff, basedpyright já removido.",
    "wayfinder migracao-ty/04 (fatia ruff check): os 18 erros pré-existentes (RUF067 non-empty-init-module x3, N818 error-suffix x2, E501 x4, RUF001/002/003 ambiguous-unicode x6, RUF059/RUF105/SIM113 x3) foram zerados por refactor mecânico preservando comportamento. `uv run ruff check src tests` → All checks passed.",
    "Triagem dos 34 skips: causa única confirmada por inspeção — todos são `skipif` sobre a ausência local de dados reais de produção (`input/2026/06/…`), usados pelos testes de aceitação/smoke que validam o engine contra CSVs reais de competência (ver docstring de tests/test_full_acceptance_smoke.py, spec.md §5). Nenhum é órfão nem esquecido; nenhum deve ser reativado, convertido em xfail ou removido. Sem mudança de código.",
    "wayfinder suite-testes/03: reparo das ~90 costuras de string acidentais (regressão do refactor) — suite foi de 16 failed/543 passed para 559 passed/34 skipped.",
    "wayfinder suite-testes/02: gate anti-regressão ISC001+ISC003 (concatenação implícita de linha única) no ruff.lint — 0 hits, sem per-file-ignores."
  ],
  "known_risks": [
    "bandit -r src reporta 3 findings low/high-confidence pré-existentes (B404/B603/B607, uso de subprocess) — fora de escopo, não relacionado a testes/tipagem.",
    "ruff format --check reporta 2 arquivos pré-existentes fora do quote-style single (ratio.py, workbook.py) — não tocados em nenhuma rodada até agora, fora do escopo de `ruff check`/`ty check`.",
    "Cobertura branch 89% acima do gate (85%), mas não auditada por risco. Módulos com branch coverage mais baixo: interactive/provider.py (56%, fronteira Questionary), log_json_sink.py (70%), orchestration/summary.py (79%), periodo.py (79%), orchestration/summary_json.py (80%)."
  ],
  "next_objective": "Cobrir os branches de maior risco em orchestration/summary.py (79% branch, lógica central de composição do relatório de conformidade) e orchestration/state.py (82%) com testes de unidade/integração focados — maior impacto em corretude de dados do que os módulos de fronteira Questionary/log-sink. O wayfinder migracao-ty está fechado (tickets 03/04); resta o ticket 05 (atualizar docs vivas: CODING_STANDARDS.md/README.md/pyguide.md de basedpyright para ty), não urgente."
}
