indicator:
  id: INMS-1.5
  contractual_id: "INMS 1.5"
  name: Disponibilidade de sistema/serviço não-crítico

scope:
  contract: "40/2022 - Ministério Cultura"
  orgao: MinC

source:
  csv: inms-001-05.csv
  delimiter: ","
  encoding: utf-8-sig

quality_gates:
  checks: []

calculation:
  shape: ratio
  aggregation: precomputed
  precomputed_result_column: "Disponibilidade Realizada (%)"

target:
  operator: ">="
  value: 99.0

penalty:
  step_points: 250
  step_size_pct: 0.1

# Anexo D (Tabela 28), INMS 1.5: meta ≥99,0% POR sistema/serviço não-crítico,
# 250 pontos a cada 0,1% abaixo da meta. /input/inms-001-05.csv real:
# Disponibilidade Realizada = 0.00% (serviço integralmente indisponível no
# período) — shortfall de 99 p.p., 990 degraus de 0,1%.
acceptance_test:
  expected:
    shape: ratio
    numerator: 0.0
    denominator: 100
    result_pct: 0.0
    conforms: false
    penalty_points: 247500.0
